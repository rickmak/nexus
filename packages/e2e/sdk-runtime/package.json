{
  "name": "@nexus/e2e-sdk-runtime",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --runInBand",
    "pretest:ci": "pnpm --filter @nexus/sdk build",
    "test:ci": "jest --runInBand --ci"
  },
  "dependencies": {
    "@nexus/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
